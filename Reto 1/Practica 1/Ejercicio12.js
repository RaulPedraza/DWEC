let base
let altura
let area
base = prompt("Introduce la base del rectangulo")
altura = prompt("Introduce la altura del rectangulo")
area = parseInt(base) * parseInt(altura)
alert ("El area del rectangulo es de " + area + " unidades")
